#################################################
# BUILD
#################################################

.job-linux-desktop:

  stage: build
  extends: .linux-prepare

  except:
    variables:
      - $DEPLOY_RUN_ANDROID
      - $DEPLOY_RUN_IOS

  script:
    - export CC=$CC
    - export CXX=$CXX
    - mkdir -p build-desktop
    - cd build-desktop
    - cmake .. -G "$CMAKE_GENERATOR" -DLINPHONESDK_PLATFORM=Desktop $DEFAULT_LINUX_CMAKE_OPTIONS -DENABLE_LIME=ON $CMAKE_OPTIONS
    - cmake --build . --target sdk -- $ADDITIONAL_BUILD_OPTIONS
  artifacts:
    paths:
      - build-desktop/*
      #UGLY ! Lime data folder isn't installed at  the right place !
      #We have to find it and add it manually...
      - lime/tester/*
    expire_in: 1 week



.scheduled-job-linux-desktop:
  extends: .job-linux-desktop
  only:
    - schedules
    
#################################################
# TESTS
#################################################

.test:
  variables:
    GIT_STRATEGY: none
    GIT_CHECKOUT: "false"
  stage: test
  allow_failure: true

.test-bctoolbox:
  extends: .test
  script:
    - cd build-desktop/WORK/desktop/Build/bctoolbox/tester
    - ls -la
    - ./bctoolbox_tester --verbose --parallel #--xml-file bcttest
  #uploading the artifacts
  artifacts:
    paths:
      - build-desktop/WORK/desktop/Build/bctoolbox/tester/BCUnitAutomated-Results.xml
    when: always
    reports:
      junit:
        - build-desktop/WORK/desktop/Build/bctoolbox/tester/BCUnitAutomated-Results.xml
    expire_in: 4 week
    
.test-bellesip:
  extends: .test
  dependencies:
    - .build-sdk
  script:
    #- ip -6 a
    #- ip -6 n
    #- ip -6 r
    - cd build-desktop/linphone-sdk/desktop/bin
    - ls -la
    - ./belle_sip_tester --verbose --parallel --resource-dir ../share/ #--xml-file bstest
  #uploading the artifacts
  artifacts:
    paths:
      - build-desktop/linphone-sdk/desktop/bin/BCUnitAutomated-Results.xml
    when: always
    reports:
      junit:
        - build-desktop/linphone-sdk/desktop/bin/BCUnitAutomated-Results.xml
    expire_in: 4 week

test-mediastreamer:
  tags: [ "docker-test-centos7-mediastreamer" ]
  extends: .test
  dependencies:
    - job-centos7-ninja-clang
  script:
    - pulseaudio -D -L module-virtual-source --exit-idle-time=-1
    - cd build-desktop/linphone-sdk/desktop/bin
    - ./mediastreamer2_tester --verbose --xml-file mstest
  #uploading the artifacts
  artifacts:
    paths:
      - build-desktop/linphone-sdk/desktop/bin/mstest-Results.xml
    when: always
    reports:
      junit:
        - build-desktop/linphone-sdk/desktop/bin/mstest-Results.xml
    expire_in: 4 week
    
test-liblinphone:
  tags: [ "docker-test-centos7-liblinphone" ]
  extends: .test
  dependencies:
    - job-centos7-ninja-clang
  script:
    # xvfb = émulation X dans un docker
    - Xvfb :99 &
    - export DISPLAY=:99
    - cd build-desktop/linphone-sdk/desktop/bin
    - ./liblinphone_tester --verbose --parallel --dns-hosts /builds/BC/public/linphone-sdk/linphone/tester/tester_hosts --show-account-manager-logs --log-file logLiblinphoneAllParThIpv6.txt
  #uploading the artifacts
  artifacts:
    paths:
      - build-desktop/linphone-sdk/desktop/bin/BCUnitAutomated*
      - build-desktop/linphone-sdk/desktop/bin/logLiblinphoneAllParThIpv6*
    when: always
    reports:
      junit:
        - build-desktop/linphone-sdk/desktop/bin/BCUnitAutomated-Results.xml
    expire_in: 4 week

.test-belr:
  extends: .test
  dependencies:
    - .build-sdk
  script:
    - cd build-desktop/linphone-sdk/desktop/bin
    - ./belr_tester --verbose --parallel --resource-dir ../share/belr-tester #--xml-file blrtest
  #uploading the artifacts
  artifacts:
    paths :
      - build-desktop/linphone-sdk/desktop/bin/BCUnitAutomated-Results.xml
    when: always
    reports:
      junit:
        - build-desktop/linphone-sdk/desktop/bin/BCUnitAutomated-Results.xml
    expire_in: 4 week

.test-belcard:
  extends: .test
  dependencies:
    - .build-sdk
  script:
    - cd build-desktop/linphone-sdk/desktop/bin
    - ./belcard_tester --verbose --parallel #--xml-file belcardtest
  #uploading the artifacts
  artifacts:
    paths :
      - build-desktop/linphone-sdk/desktop/bin/BCUnitAutomated-Results.xml
    when: always
    reports:
      junit:
        - build-desktop/linphone-sdk/desktop/bin/BCUnitAutomated-Results.xml
    expire_in: 4 week
    
.test-bzrtp:
  extends: .test
  dependencies:
    - .build-sdk
  script:
    - cd build-desktop/WORK/desktop/Build/bzrtp/test
    - ls -la
    #We must tell it that the ressources it wants are in its own folder
    #different behaviour from other testers
    - ./bzrtpTest --verbose --parallel --resource-dir . #--xml-file bzrtptest 
  #uploading the artifacts
  artifacts:
    paths :
      - build-desktop/WORK/desktop/Build/bzrtp/test/BCUnitAutomated-Results.xml
    when: always
    reports:
      junit:
        - build-desktop/WORK/desktop/Build/bzrtp/test/BCUnitAutomated-Results.xml
    expire_in: 4 week
    

#need to fix the yum slowdowns causing the job to fail, and to update the dockerfile
test-lime:
  tags: [ "docker-test-centos7-lime" ]
  extends: .test
  dependencies:
    - job-centos7-ninja-clang
  script:
    - ls -la .
    - pwd
    - ls -la ./lime
    - cd /builds/BC/public/linphone-sdk/lime/tester/server/nodejs
    #installing here since the tester cannot find globally installed packages
    - npm install yargs sqlite3 rwlock
    - ./localServerStart.sh & #How do we stop it properly ????
    - cd /builds/BC/public/linphone-sdk
    - cd build-desktop/WORK/desktop/Build/
    - ls -la
    - cd lime
    - ls-la
    - cd tester
    - ls -la

    #path to source dir /builds/BC/public/linphone-sdk/lime
    - ./lime_tester --verbose --xml-file limetest --resource-dir ../../../../../../lime/tester
  #uploading the artifacts
  artifacts:
    paths:
      - build-desktop/WORK/desktop/Build/lime/tester/limetest-Results.xml
    when: always
    reports:
      junit:
        - build-desktop/WORK/desktop/Build/lime/tester/limetest-Results.xml
