variables:
  GIT_SUBMODULE_STRATEGY: recursive
  MAKEFILE_JOBS: 5
  CCACHE_SIZE: 2G


stages:
 - build
 - test


.linux-ccache:

  cache:
    key: $CI_JOB_NAME
    paths:
      - ccache/

  before_script:
    - mkdir -p ccache
    - echo "max_size = $CCACHE_SIZE" > ccache/ccache.conf
    - export CCACHE_BASEDIR=${PWD}
    - export CCACHE_DIR=${PWD}/ccache
    - ccache -s

  after_script:
    - export CCACHE_DIR=${PWD}/ccache
    - ccache -s

#fixed dot for visible job
build-sdk:
  tags: [ "docker-centos7" ]
  image: gitlab.linphone.org:4567/bc/public/linphone-sdk/bc-dev-centos:7
  variables:
    CMAKE_GENERATOR: Unix Makefiles
    CC: gcc
    CXX: g++
  stage: build
  extends: .linux-ccache

  script:
    - export CC=$CC
    - export CXX=$CXX
    - mkdir -p build-desktop
    - cd build-desktop
    - cmake .. -G "$CMAKE_GENERATOR" -DLINPHONESDK_PLATFORM=Desktop $DEFAULT_LINUX_CMAKE_OPTIONS $CMAKE_OPTIONS
    - cmake --build . --target sdk -- $ADDITIONAL_BUILD_OPTIONS
    #- find . -name "tester_hosts"
  artifacts:
    paths:
      - build-desktop/*
    expire_in: 1 week



#.scheduled-job-linux-desktop:
#  extends: .job-linux-desktop
#  only:
#    - schedules

test-bctoolbox:
  tags: [ "docker-centos7" ]
  variables:
    GIT_STRATEGY: none
    GIT_CHECKOUT: "false"
  stage: test
  script:
    - find ../../.. -name "bctoolbox_tester"
    - cd linphone-sdk/desktop/linphone-sdk/build-desktop/WORK/desktop/Build/bctoolbox/tester
    - ls -la
    - ./bctoolbox_tester --verbose --xml-file bcttest
#uploading the artifacts for the next stage
  artifacts:
    when: always
    reports:
      junit:
        - linphone-sdk/desktop/build-desktop/linphone-sdk/desktop/bin/bcttest-Results.xml

test-bellesip:
  tags: [ "docker-centos7" ]
  variables:
    GIT_STRATEGY: none
    GIT_CHECKOUT: "false"
  stage: test
  script:
    - find . -name "belle_sip_tester"
    - cd linphone-sdk/desktop/build-desktop/linphone-sdk/desktop/bin
    - ls -la
    - ./belle_sip_tester --verbose --resource-dir ../share/ --xml-file bstest
#uploading the artifacts for the next stage
  artifacts:
    when: always
    reports:
      junit:
        - linphone-sdk/desktop/build-desktop/linphone-sdk/desktop/bin/bstest-Results.xml
        
test-mediastreamer:
  tags: [ "docker-centos7" ]
  variables:
    GIT_STRATEGY: none
    GIT_CHECKOUT: "false"
  stage: test
  script:
    - sudo yum install -y alsa-utils pulseaudio
    - pulseaudio -D -L module-virtual-source --exit-idle-time=-1
    - cd linphone-sdk/desktop/build-desktop/linphone-sdk/desktop/bin
    - ./mediastreamer2_tester --verbose --xml-file mstest
#uploading the artifacts for the next stage
  artifacts:
    when: always
    reports:
      junit:
        - linphone-sdk/desktop/build-desktop/linphone-sdk/desktop/bin/mstest-Results.xml